export 'package:gadfly_flutter_template/external/effect_providers/mixpanel/effect_fake.dart';

/// This is an exception, but we don't need to crete a mock for this effect
/// because one already exists in the application code.
